/**
 * 人気路線 Top 20（利用者数順・固定）
 * LINES への参照はこのファイルが初めてimportされた時点で解決される。
 * Reactのレンダリングサイクルと完全に無関係。
 */
import { LINES } from './mockData';
import type { Line } from './types';

const POPULAR_LINE_IDS: string[] = [
  'g11302',  // 1  JR山手線
  'g25001',  // 2  小田急線（小田原線）
  'g11312',  // 3  JR中央線(快速)
  'g26003',  // 4  東急田園都市線
  'g11301',  // 5  JR東海道本線(東京〜熱海)
  'g28004',  // 6  東京メトロ東西線
  'g24001',  // 7  京王線
  'g11332',  // 8  JR京浜東北線
  'g11313',  // 9  JR中央・総武線
  'g26001',  // 10 東急東横線
  'g99618',  // 11 大阪メトロ御堂筋線
  'g11319',  // 12 宇都宮線
  'g11323',  // 13 JR高崎線
  'g21002',  // 14 東武伊勢崎線（スカイツリーライン）
  'g22001',  // 15 西武池袋線
  'g28005',  // 16 東京メトロ千代田線
  'g11320',  // 17 JR常磐線(上野〜取手)
  'g34001',  // 18 阪急神戸本線
  'g11602',  // 19 JR京都線(京都〜大阪)
  'g28002',  // 20 東京メトロ丸ノ内線
];

// LINES はこのモジュールが評価される時点（= アプリ起動直後）で
// 完全にロード済みのため、find() は必ず正しい結果を返す。
export const POPULAR_LINES: Line[] = POPULAR_LINE_IDS
  .map((id) => LINES.find((l) => l.id === id))
  .filter((l): l is Line => l !== undefined);
